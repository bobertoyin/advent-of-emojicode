# advent of emojicode

```sh
# compile shared package
make compile-shared

# compile shared package and solution
make compile year=<YEAR> day=<DAY>

# execute compiled solution
make execute year=<YEAR> day=<DAY>

# compile + execute
make run
```
